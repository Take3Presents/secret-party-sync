{
  "name": "secret-party-sync",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "sync": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
